<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.patrickkorianski.adma_madlibs">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainMenu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".EasyLibActivity" />
        <activity android:name=".MedLibActivity" />
        <activity android:name=".HardLibActivity" />
        <activity android:name=".InstructLibActivity" />
        <activity android:name=".EasyLibSelection" />
        <activity android:name=".EasyLibFinal" />
        <activity android:name=".MedLibSelection" />
        <activity android:name=".MedLibFinal"></activity>
    </application>

</manifest>
